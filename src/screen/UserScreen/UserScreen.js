import { SafeAreaView, Text } from 'react-native';
function UserScreen() {
    return (
        <SafeAreaView>
            <Text>User Setting</Text>
        </SafeAreaView>
    );
}

export default UserScreen;
