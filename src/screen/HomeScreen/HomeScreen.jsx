import {
    Image,
    StyleSheet,
    Text,
    View,
    ScrollView,
    SafeAreaView,
    TextInput,
    TouchableOpacity,
    Platform,
} from 'react-native';
import { StatusBar } from 'expo-status-bar';

function HomeScreen() {
    return (
        <SafeAreaView>
            <Text>Home Screen</Text>
        </SafeAreaView>
    );
}

export default HomeScreen;
